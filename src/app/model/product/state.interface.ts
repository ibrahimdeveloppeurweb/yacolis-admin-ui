import {ProductType} from "./type.interface";

export interface ProductState {
    id?: string,
    uuid?: string,
    label?: string,
    status: boolean,
    created_at?: string,
    updated_at?: string,
    create?: string,
    update?: string
  }
  